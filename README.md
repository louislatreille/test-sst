Example of usage:

```
curl -X POST -H 'Content-Type: application/json' https://<your_rest_api_url>/messages -d '{ "name": "Louis", "message": "Hello Serverless Streamlined!" }'
curl -X GET https://<your_rest_api_url>/messages?name=Louis
```
